# ES6 정리

1. 클래스

- ES6에서 도입된 객체를 생성하기 위한 새로운 설계도 양식이다.
- 기존의 생성자 함수보다 훨씬 더 명확하고 체계적으로 객체를 설계할 수 있다.

<pre>
<code>
class Student{
    constructor(name, group){
        this.name = name;
        this.group = group;
    }

    introduce(){
        console.log(`안녕하세요 ${this.group}반 ${this.name}입니다.`)
    }
}

const studentA = new Student('홍길동', 'A')
const studentB = new Student('유관순', 'B')

// 모든 인스턴스가 같은 함수를 쓴다.
console.log(studentA.introduce === studentB.introduce);
</code</pre>

2. 화살표 함수

- 기본 변환
    <pre>
    <code>
    const message = function() {
        return 'hello';
    }
  
    const arrowMsg = () => {
        return 'hello
    }
  
    //본문이 return 한줄이면, 중괄호와 return 동시 생략 가능
    const arrowMsg2 = () => 'hello'
  
    //매개변수가 하나일 때, 소괄호 생략 가능
    const arrowPower = x => x*X;
  
    //매개변수가 없거나, 두 개 이상이면 소괄호() 필수
    const sum = (x,y) => x+y;
  
    // 주의!
    // 객체 리터럴을 반환할 때는 소괄호로 감싸야 한다.
    // 감싸지 않으면 함수 몸체의 중괄호{}로 오해함
    const numbers = [1,2,3,4,5]
    const creatUser = (id, name) => ({id, name})
  
    //고차 함수와 쓸 때 위력이 극대화 된다.
    //const mappedNumbers = numbers.map(function (val) {
        return val * 10;
    })
  
    const mappedNumber = numbers.map((val) => val * 10);
    console.log(mappedNumber) // [10,20,30,40,50]
    </code>
    </pre>

- 일반 함수의 this는 호출할 때 결정된다.
- 즉, 누가 부르냐에 따라 바뀜
- 하지만 화살표 함수는 자신만의 this를 가지지 않고, 바깥 스코프의 this를 그대로 쓴다.
- 객체 메서드 = 무조건 일반 함수 사용 / 화살표 함수 사용시 this 깨짐

3. 이터러블

- 순회 가능한
- for...of 반복문 사용 가능
- Array.from()을 사용하면 진짜 배열로 변환 가능

<pre>
    <code>
      const array = ['사과', '바나나','딸기']
      for( const item of array){
        console.log(item)
      }
    </code>
    </pre>

4. Rest parameter, spread syntax

- Rest Parameter: 여러 개의 값을 하나의 배열로 모으기
- Spread Syntax: 하나의 배열을 여러 개의 값으로 펼치기

- Rest 파라미터는 반드시 매개변수 목록의 가장 마지막에 위치해야 한다.
<pre>
    <code>
      function merge(first, ...args){
        console.log(`첫 번째: ${first}`);
        console.log("나머지(배열):", args);

        let message = first;
        for (let arg of args) {
            message += arg;
        }
         return message;
      }

      console.log(merge("안녕하세요"));
      console.log(merge("안녕하세요 ", "반갑습니다 ", "저는 은서입니다."));
    </code>
    </pre>

- Spread Syntax
- 함수의 '인수' 자리나, 배열/객체 리터럴 안에서 사용하여
- 배열의 요소들을 개별 값의 목록으로 펼쳐준다.

 <pre>
 <code>
    const numbers = [10, 20, 30]
    console.log(Math.max(...numbers));

    //배열 리터럴에서 사용(합치기/복사)
    const arr1 = [1,2,3];
    const arr2 = [4,5,6];

    const mergedArray = [...arr1, ...arr2, 7,8]
    console.log(mergedArray)

    //배열 복사시 주소값은 복사되지 않음 
 </code>
 </pre>

5. 구조 분해 할당

- 배열이나 객체의 속성을 분해하여 그 값을 개별 변수에 쉽게 담을 수 있다.

<pre><code>
    const nameArr = ['panda', 'kwon']

    //기존 방식
    <!-- const firstName = nameArr[0];
    const lastName = nameArr[1]; -->

    // 구조 분해 할당 방식 - 배열을 순서가 기준
    const [firstName, lastName] = nameArr;
    console.log(`성: ${lastName} 이름: ${firstName}`);

    //쉼표를 사용해 특정 요소 건너뛰기
    const [, name] = ['kwon', 'panda']

    //rest 파라미터로 나머지 요소들을 새로운 배열로 모을 수 있음
    const [leader, ...members] = ["팀장", "팀원1", "팀원2", "팀원3"];
    console.log(leader);
    console.log(members);

    // 기본값 설정: 할당할 값이 없을 때 사용될 기본값 지정
    const [user1, user2 = "기본값"] = ["사용자1"];
    console.log(user1);
    console.log(user2);

    // 객체 구조 분해 할당
    // 객키는 키(key)이름이 중요하다. 변수이름과 같은 기를 찾아 값이 할당됨
    // 순서는 중요하지 않음

    const student = {
        name1: "판다",
        age: "4",
        major: "영화감상",
    };

    // 콜론(:)을 사용하면, 'name1' 키의 값을 'name'이 아닌 'studentName'이라는 새 변수명으로 할당 가능
    const { name1: studentName, age } = student;
    // console.log(`이름: ${name1} 나이: ${age}`);
    console.log(`이름: ${studentName} 나이: ${age}`);

    // 기본값(=) 설정
    const { name1: sName, job = "학생" } = student;
    // console.log(job);

    // rest 파라미터로 나머지 프로퍼티들을 새로운 객체로 모을 수 있다.
    const { age: studentAge, ...restInfo } = student;
    // console.log("-------------------");
    console.log(restInfo);


    /**
 * 함수 매개변수 구조 분해
 * 함수에 객체를 인자로 전달할 때, 구조 분해 할당을 사용하면
 * 매개변수의 순서를 신경 쓸 필요 없고, 코드의 가독성이 매우 높아진다.
 */

const product = {
  id: "p-001",
  name: "노트북",
  price: "1,500,000",
  spec: {
    cpu: "i7",
    ram: "16gb",
  },
};

// function printProductInfo(product) {
//   console.log(`상품아이디: ${product.id}`);
//   console.log(`가격: ${product.price}`);
// }

function printProductInfo({ id, price, spec: { ram }, producer = "삼성" }) {
  console.log(`상품아이디: ${id}`);
  console.log(`상품 가격: ${price}`);
  console.log(`RAM: ${ram}`);
  console.log(`제조사: ${producer}`);
}

printProductInfo(product);
</code></pre>
