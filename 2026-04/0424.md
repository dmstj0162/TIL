## Zustand 사용하기

-> React에서 전역 상태를 쉽게 관리하기 위한 라이브러리

- 여러 컴포넌트에서 공통으로 사용하는 데이터를 관리할 때 사용
- 예: 로그인 정보(user), 장바구니, 테마 등

## 왜 쓰는가?

- React 기본 방식은
  👉 부모 → 자식으로 props 계속 내려야 함 (props drilling)
- zustand 쓰면
  👉 어디서든 바로 상태 가져다 쓸 수 있음
