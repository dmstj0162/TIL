## Security

- Spring Security는 스프링 기반 애플리케이션의 보안(인증과 권한, 인가 등)을 담당하는 스프링 프레임워크이다.
- Security는 인증과 권한에 대한 부분을 Filter의 흐름에 따라서 처리를 하며, 보안과 관련된 옵션을 체계적으로 제공하므로 개발자가 일일이 보안과 관련된 로직을 작성하지 않아도 되는 장점을 가지고 있다.

## 용어 정의

1. Principal : 주체

- 사용자, 디바이스, 시스템 등의 행위를 하는 주체를 의미

2. Secured Resource : 보안이 적용되는 리소스

- 일반적으로 메서드를 통해 보호가 된다.

3. Authentication : 인증

- Principal이 믿을 수 있는지 파악하는 것으로 일반적으로 id/pwd를 검사하게 된다.

4. Authorization : 권한

- 인증이 완료된 principal이 어떤 행위를 할 권한이 있는지 확인한다.
- ROLE_ADMIN, ROLE_GUEST, ROLE_MEMBER의 role에 기반해서 접근이 가능하다.

5. SSR : server side rendering

- 서버에서 다 만들어서 제공함 -> 타임리프, SSO
- 서버 과부하로 속도가 느림

6. CSR : client side rendering
7. JSP : java server page > servlet -->>> boot, springfreamwork handler method
8. Cookie : 로컬에 데이터가 저장

## 세션 인증 vs 토큰(JWT) 인증

- 상태를 서버가 관리할 것인가, 클라이언트가 관리할 것인가

## filter와 interceptor의 차이점?

- 가로채기인데, filter는 서버에 요청이 들어왔을 때 잡아서 쓰는 것, interceptor는 springfreamWork꺼이기 때문에 filter와 다르게 spirng bean 객체에 접근이 가능하다. 에러처리

## CQRS

- Command와 Query를 나뉨
