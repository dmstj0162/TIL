## Multipart

multipart/form-data
= 파일과 일반 값을 함께 보내는 요청 형식

multipartFile
= Spring에서 업로드 파일을 표현하는 객체

@RequestParam(file) MultipartFile file
= multipart 요청의 file part를 받는 방법

React가 formData로 보내면 Spring은 multipartFile로 받는다.
