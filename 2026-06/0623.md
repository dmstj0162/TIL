### 인증/인가

- 서버는 세션에 저장, 토큰은 로컬에 저장됨
- 인증/인가를 해결하기 위해 springboot에 있는 security를 가져다가 쓸 것

### secretKey 값은 .yaml 파일에서 관리

### SecurityConfig

- 안에는 @Configuration, @EnableWebSecurity가 들어있음

1. passwordEncoder라는 @Bean 등록
2. Filter를 태운다 -> 세션로그인이 아닌 토큰 로그인 설정
