# Python 기초 학습 정리 (Day 1)

## 학습 내용

- 변수 (Variable)
- 데이터 타입 (Datatype)
- 숫자형 (Number)
- 불린(Boolean)
- 문자열(String)
- 리스트(List)
- 튜플(Tuple)
- 딕셔너리(Dictionary)
- 집합(Set)

---

# 1. 변수 (Variable)

## 변수란?

데이터를 저장하기 위한 메모리 공간에 이름을 붙인 것

```python
name = "홍길동"
age = 20
```

### 변수명 작성 규칙

- 영문자, 숫자, \_(언더바) 사용 가능
- 숫자로 시작 불가
- 대소문자 구분
- 예약어 사용 불가

```python
student_name = "Tom"
```

---

# 2. 데이터 타입 (Datatype)

파이썬은 변수 선언 시 자료형을 따로 지정하지 않는다.

```python
a = 10
b = 3.14
c = "Python"
d = True
```

자료형 확인

```python
print(type(a))
```

주요 자료형

| 자료형 | 설명     |
| ------ | -------- |
| int    | 정수     |
| float  | 실수     |
| str    | 문자열   |
| bool   | 참/거짓  |
| list   | 리스트   |
| tuple  | 튜플     |
| dict   | 딕셔너리 |
| set    | 집합     |

---

# 3. 숫자(Number)

## 정수

```python
num = 100
```

## 실수

```python
pi = 3.14
```

## 산술 연산자

```python
+
-
*
/
%
//
**
```

예시

```python
10 + 3
10 - 3
10 * 3
10 / 3
10 // 3
10 % 3
10 ** 3
```

---

# 4. Boolean

참(True)과 거짓(False)을 표현

```python
isLogin = True
isAdmin = False
```

비교 연산

```python
10 > 5
10 < 5
10 == 10
10 != 5
```

논리 연산

```python
and
or
not
```

---

# 5. 문자열(String)

## 문자열 생성

```python
name = "Python"
```

또는

```python
name = 'Python'
```

## 문자열 인덱스

```python
text = "hello"

text[0]
text[1]
```

## 슬라이싱

```python
text[1:4]
```

결과

```
ell
```

형식

```python
[start:end:step]
```

예시

```python
text[:5]
text[2:]
text[::-1]
```

## 문자열 함수

```python
upper()
lower()
replace()
split()
find()
count()
strip()
```

---

# 6. List

여러 개의 데이터를 저장하는 자료형

```python
fruits = ["apple", "banana", "orange"]
```

## 특징

- 순서 O
- 수정 O
- 중복 O

### 요소 접근

```python
fruits[0]
```

### 추가

```python
append()
insert()
extend()
```

### 삭제

```python
remove()
pop()
del
```

### 길이

```python
len(fruits)
```

---

# 7. Tuple

리스트와 비슷하지만 수정 불가능

```python
color = ("red", "blue", "green")
```

특징

- 순서 O
- 수정 X
- 중복 O

사용 이유

- 변경되면 안 되는 데이터 저장

예시

```python
days = ("Mon","Tue","Wed")
```

---

# 8. Dictionary

Key와 Value로 데이터를 저장

```python
person = {
    "name":"홍길동",
    "age":20
}
```

데이터 접근

```python
person["name"]
```

추가

```python
person["height"] = 180
```

삭제

```python
del person["age"]
```

주요 함수

```python
keys()
values()
items()
get()
```

---

# 9. Set

중복을 허용하지 않는 자료형

```python
numbers = {1,2,3,4}
```

특징

- 순서 X
- 중복 X

추가

```python
add()
```

삭제

```python
remove()
discard()
```

집합 연산

```python
합집합 |

교집합 &

차집합 -
```

---

# 자료형 비교

| 자료형     | 순서 | 수정 | 중복  |
| ---------- | ---- | ---- | ----- |
| List       | O    | O    | O     |
| Tuple      | O    | X    | O     |
| Dictionary | Key  | O    | Key X |
| Set        | X    | O    | X     |

---

# 오늘 배운 핵심

- 변수는 데이터를 저장하는 공간이다.
- 파이썬은 자료형을 자동으로 결정한다.
- 문자열은 인덱스와 슬라이싱이 가능하다.
- List는 수정 가능하다.
- Tuple은 수정이 불가능하다.
- Dictionary는 Key-Value 구조이다.
- Set은 중복을 제거한다.
- 각 자료형은 목적에 맞게 사용해야 한다.

---

# 자주 사용하는 함수

```python
type()
len()
print()
input()

append()
insert()
remove()
pop()

upper()
lower()
replace()
split()

keys()
values()
items()
get()
```

---

# 다음에 공부하면 좋은 내용

- 조건문 (if)
- 반복문 (for, while)
- 함수(Function)
- 예외 처리(Exception)
- 파일 입출력(File I/O)
