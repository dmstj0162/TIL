## 추상클래스 특징

1. abstract class
2. extends
3. 단일 상속만
4. 일반 메서드 가능
5. 변수-일반필드 가능
6. 목적 : 공통 기능+강제

## 인터페이스 특징

1. interface
2. implements
3. 여러개 구현 가능
4. default 메서드로만
5. 상수만 가능
6. 목적 : 순수 규격/계약 정의

## 파싱

- 문자열 데이터를 실제 해당 타입의 기본 자료형으로 변환
  <code>

      String heighStr = "170.6";
      -> double height = Double.parseDouble(heigh);

</code>

## 주의 사항

- Wrapper 클래스는 '객체'이기 때문에, 값 비교를 할 때 == 연산자를 쓰면 위험, 반드시 equals 사용!

## Generic

- <T> : 타입 파라미터(Type Parameter). T는 타입을 담는 변수 자리
- 이 클래스를 사용할 때 T자리에 실제 타입을 넣는다
- 제네릭에 들어가는 타입은 반드시 '객체 타입(참조형)'이어야 한다.
- 내부적으로 타입을 Object로 다루기 때문에, 기본형은 Object로 업캐스팅 할 수 없다.

<code>

    public class GenericBox <T> {
        private T content;

        public T getContent(){
            return content;
        }

        public void setContent(T content){
            this.content = content;
        }
    }

</code>

## Comparator: 클래스 밖에서 '별도의 정렬 기준'을 정의할 떄 사용

<code>
    public class AscendingPrice implements Cmparator<BookDTO>{

        @override
        public int compare(BootDTO o1, BookDTO o2){
            return Integer.compare(o1.getPrice(), 02.getPrice())
        }
    }

</code>

## Comparable<BookDTO> : 이 클래스는 BookDTO끼리 크기 비교가 가능하다는 계약

## 정렬 기준 정의

- 반환값 규칙: 음수 -> 현재(this)객체가 앞으로 / 양수 -> 뒤로 / 0 -> 동등
