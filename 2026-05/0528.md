memberService 호출
-> Spring Proxy 가 중간에서 가로챔
-> Pointcut 조건에 맞는지 확인
-> Advice 실행
-> 실제 MemberService 메서드 실행
-> Advice 후 처리
-> 결과 반환 또는 예외 전달

## AOP(Aspect Oriented Programming)

- 관점 지향 프로그래밍의 약자
- 중복되는 공통 코드를 분리하고 코드 실행 전이나 후의 시점에 해당 코드를 삽입함으로써 소스 코드의 중복을 줄이고, 필요할 때마다 쓸 수 있게 객체화 하는 기술을 말함

## AOP 핵싱 용어 --- 강사님이 불러주신 내용으로 다시 정리

## Advice의 종류

1. Before : 대상 메소드가 실행되기 이전에 실행되는 어드바이스
2. After-returning : 대상 메소드가 정상적으로 실행된 이후에 실행되는 어드바이스
3. After-throwing : 예외가 발생했을 때 실행되는 어드바이스
4. After : 대상 메소드가 실행된 이후에(정상, 예외 관계없이) 실행되는 어드바이스
5. Around : 대상 메소드 실행 전/후에 적용되는 어드바이스

## Spring Boot 특징

1. 자동설정

- 필요한 기본 설정을 Spring Boot가 알아서 구성

2. Starter 의존성

- 웹 개발에 필요한 라이브러리 묶음을 한 번에 가져온다

3. 내장 서버

- Tomcat을 따로 설치하지 않아도
- main 메서드로 웹 서버 실행 가능

- HTML 응답시 @Controller 사용
- JSON 응답시 @RestController 사용
