## SUBQUERIES(서브쿼리)

- 괄호안의 서브 쿼리가 먼저 실행됨
- 서브쿼리 결과가 여러개라면 IN 연산자 사용
<pre>
<code>

SELECT
menu_name,
category_code
FROM
tbl_menu
WHERE
category_code= (SELECT
category_code
FROM
tbl_menu
WHERE
menu_name = '민트미역국');
</code>

</pre>

## DDL

- CREATE TABLE
- ALTER TABLE(ADD, DROP, CHANGE, MODIFY ...)
- DROP TABLE(값, 테이블 지우기)
- TRUNCATE TABLE(값만 지우기)

## CONSTRAINTS(제약 조건)

1. NOT NULL : NULL값 허용하지 않음
2. UIQUE : 중복값 허용하지 않음
3. PRIMARY KEY(기본키)
   - 테이블에서 한 행의 정보를 찾기 위해 사용할 컬럼을 의미
   - 테이블에 대한 식별자 역할을 함(한 행씩 구분하는 역할)
   - NOT NULL + UNIQUE 제약조건의 의미
4. FOREIGN KEY(외래키)
   - 두 테이블을 연결하고 관계를 맺어준다.
   - 참조된 다른 테이블에서 제공하는 값만 사용할 수 있다.
   - 제공되는 값 외에는 NULL 사용 불가
5. CHECK
   - 들어올 수 있는 값의 범위나 조건을 직접 지정
6. DEFAULT
   - INSERT시 특정 컬럼에 값을 주지 않으면, 자동으로 채워질 기본값을 정한다.

## DATA TYPE

- 형변환(실수 -> 정수)
- CAST(값 AS 바꿀타입) : 값을 특정 데이터 타입으로 바꾸는 함수, 원본 데이터를 수정하는 것은 아님
